source ./scripts/utils.sh

export PROJECT=yin
export VERSION=$(get_version)

# GCP Artifact Registry Configuration (optional)
# Uncomment and configure these if publishing to GCP Artifact Registry
# Uses application default credentials (gcloud auth application-default login)
# For other registries, update the publish recipe in justfile
export GCP_PROJECT_ID="devops-466002"
export GCP_REGION="us-east1"
export GCP_REPOSITORY="cloudvoyant-generic-registry"
